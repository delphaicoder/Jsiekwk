# v1.6.11

- Fixed the Pin widget glyph appearing as a square by using a battery glyph.
- Fixed Weather/Text values being written before the text editor was completed. Type + custom text are now saved together when the user presses Lưu.
- Widget reload logs now include all three slot types and custom text values, making config-sync problems easier to diagnose.
- Kept the v1.5.6 cutout engine untouched.
